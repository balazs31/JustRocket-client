export class AppSettings {
    public static API = {
        HOST: "http://localhost",
        PORT: 4200,
        ENDPOINTS: {
            UPLOAD: "/uploadFile",
            DOWNLOAD: "/downloadFile/",
            LOGIN: "/login"
        }
    }
}
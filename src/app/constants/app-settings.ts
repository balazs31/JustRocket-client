export class AppSettings {
    public static API = {
        HOST: "http://localhost",
        PORT: 8080,
        ENDPOINTS: {
            UPLOAD: "/uploadFile",
            DOWNLOAD: "/downloadFile/",
            LOGIN: "/login",
            GETFILES: "/getFiles"
        }
    }
}